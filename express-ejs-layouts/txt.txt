async function loadContact() {
  try {
    const { rows } = await pool.query("SELECT name, phone_number FROM contact");
    return contacts;
  } catch (err) {
    console.error(err.message)
  }
}

membuat aplikasi contact kemarin menjadi efisien dan menjadi react